name: OWASP Dependency Check

on:
  push:
    branches: [main]

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: OWASP Dependency Check
        uses: dependency-check/Dependency-Check_Action@f4b225b2441a8df200780a2918d53b2b2e2ee80d # v3.0.0
        with:
          project: 'PortfolioAngular'
          path: '.'
          format: 'HTML'
          out: 'reports'

      - name: Upload rapport
        uses: actions/upload-artifact@v4
        with:
          name: rapport-owasp
          path: reports/